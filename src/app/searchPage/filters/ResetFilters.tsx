const ResetFilters = () => {
    const resetFilters = () => {
        // noop
    }

    return (
        <button
            className="ml-5 font-semibold text-primary"
            onClick={resetFilters}
        >
            Reset filters
        </button>
    )
}

export default ResetFilters
